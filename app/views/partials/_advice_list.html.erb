<div>
  <table class="table">
    <tr>
      <th>advice</th>
      <th>author</th>
    </tr>
    <% messages.each do |m| %>
    <tr>
      <td><%= m.advice %></td>
      <td><%= link_to m.user.name, user_path(m.user) %></td>
    </tr>
    <% end %>
  </table>
</div>
